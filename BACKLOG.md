# Backlog

## Features

- **`spin.sh` list mode** — Add a flag/subcommand to list existing dev containers
- **Dev container deletion** — Add a delete subcommand to `spin.sh` with a strong warning before removing containers/volumes
- **README for end users** — Write user-facing documentation covering setup, usage, and common workflows
- **Pre-configured p10k** — Ship a fixed Powerlevel10k config so the setup wizard never runs in a fresh container
- **tmux mouse mode** - Activate tmux mouse mode by default
