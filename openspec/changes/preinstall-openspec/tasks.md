## 1. Dockerfile

- [x] 1.1 Add `@fission-ai/openspec` to the `npm install -g` line in the Dockerfile

## 2. Specs

- [x] 2.1 Merge the `openspec-cli` delta into `openspec/specs/dev-container-image/spec.md`

## 3. Verification

- [x] 3.1 Rebuild the container image and verify `openspec --version` runs without error as the `dev` user
